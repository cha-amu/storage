{
  "name": "cha-amu-storage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-storage-repo.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
